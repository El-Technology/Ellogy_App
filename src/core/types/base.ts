export type SignInType = {
  email: string;
  password: string;
};

export type SignUpType = {
  firstName: string,
  lastName: string,
  email: string,
  phoneNumber: string
  organization: string,
  department: string,
};